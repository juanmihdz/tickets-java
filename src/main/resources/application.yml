spring:
  application:
    name: supportportal

  datasource:
    url: jdbc:postgresql://localhost:5432/supportportal
    username: postgres
    password: GreedIsland!
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      # En producción, no confiaría en eso y usaría migraciones con Flyway o Liquibase.
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8081